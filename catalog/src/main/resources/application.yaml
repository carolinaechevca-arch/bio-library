server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: catalog

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/biolibrary}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
